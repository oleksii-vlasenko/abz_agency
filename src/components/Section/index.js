import React from "react";
import HomePage from "./HomePage";

const Section = () => {
    return(
        <React.Fragment>
            <HomePage />
        </React.Fragment>
    );
};

export default Section;
